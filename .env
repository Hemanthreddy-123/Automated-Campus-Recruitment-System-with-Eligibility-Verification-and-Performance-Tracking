# Local development — points to local backend
VITE_API_URL=http://localhost:5000/api

# Production — Render backend (used by Netlify build)
# VITE_API_URL=https://automated-campus-recruitment-system-with.onrender.com/api
